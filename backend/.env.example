PORT=5000

# Local MySQL defaults
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_local_password
DB_NAME=brightlearn_lms

# Set to true for Aiven or other TLS-enabled MySQL providers
DB_SSL=false

# Absolute path or path relative to the backend folder
DB_SSL_CA_PATH=

JWT_SECRET=replace_with_secure_jwt_secret
JWT_REFRESH_SECRET=replace_with_secure_refresh_secret
HF_API_KEY=
HF_MODEL=deepseek-ai/DeepSeek-R1:fastest
